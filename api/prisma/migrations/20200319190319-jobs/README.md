# Migration `20200319190319-jobs`

This migration has been generated by Sara Vieira at 3/19/2020, 7:03:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."User" (
    "email" TEXT NOT NULL DEFAULT '' ,
    "email_verified" BOOLEAN NOT NULL DEFAULT false ,
    "family_name" TEXT NOT NULL DEFAULT '' ,
    "given_name" TEXT NOT NULL DEFAULT '' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "locale" TEXT NOT NULL DEFAULT '' ,
    "picture" TEXT NOT NULL DEFAULT '' ,
    "sub" TEXT NOT NULL DEFAULT '' ,
    "updated_at" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' 
) 

CREATE TABLE "quaint"."Task" (
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "price" INTEGER NOT NULL DEFAULT 0 ,
    "task_name" TEXT NOT NULL DEFAULT '' ,
    "type" TEXT NOT NULL DEFAULT 'HOUR' ,
    "work" INTEGER   ,FOREIGN KEY ("work") REFERENCES "Work"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."Work" (
    "can_contact" BOOLEAN NOT NULL DEFAULT false ,
    "description" TEXT NOT NULL DEFAULT '' ,
    "donate_link" TEXT NOT NULL DEFAULT '' ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "user" INTEGER NOT NULL  ,FOREIGN KEY ("user") REFERENCES "User"("id") ON DELETE RESTRICT ON UPDATE CASCADE
) 

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200319190319-jobs
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,44 @@
+datasource DS {
+  provider = "sqlite"
+  url      = env("DATABASE_URL")
+}
+
+generator photonjs {
+  provider      = "prisma-client-js"
+  binaryTargets = env("BINARY_TARGET")
+}
+
+// Define your own datamodels here and run `yarn redwood db save` to create
+// migrations for them.
+model User {
+  id             Int      @id @default(autoincrement())
+  email          String   @unique
+  given_name     String
+  email_verified Boolean
+  family_name    String
+  picture        String
+  locale         String
+  updated_at     DateTime
+  sub            String
+}
+
+enum Type {
+  HOUR TASK
+}
+
+model Task {
+  id        Int    @id @default(autoincrement())
+  task_name String
+  price     Int
+  type      Type
+}
+
+
+model Work {
+  id          Int     @id @default(autoincrement())
+  can_contact Boolean
+  user        User
+  donate_link String
+  description String
+  tasks       Task[]
+}
```


